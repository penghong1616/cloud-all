server:
  port: 84
  context-path: /consumer
feign:
  hystrix:
    enabled: true
#服务信息
info:
  app.name: consumer-service
  company.name: ngos
  build.artifactId: $project.artifactId$
  build.version: $project.version$