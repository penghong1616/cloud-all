server:
  port: 83
#feign配置
feign:
  httpclient:
    enabled: true
  hystrix:
    enabled: true
hystrix:
  dashboard:
    proxy-stream-allow-list: "*"
#服务信息
info:
  app.name: hytrixmonitor-service
  company.name: ngos
  build.artifactId: $project.artifactId$
  build.version: $project.version$
management:
  security:
    enabled: false
security:
    user:
      name: admin
      password: admin
